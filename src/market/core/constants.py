

# WebSocket 配置
WS_RECONNECT_INTERVAL = 5  # 重连间隔(秒)
WS_HEARTBEAT_INTERVAL = 30  # 心跳间隔(秒)
WS_TIMEOUT = 10  # 超时时间(秒)

# 数据配置
MAX_ORDERBOOK_DEPTH = 20  # 最大订单簿深度
SNAPSHOT_INTERVAL = 100  # 快照生成间隔(ms)